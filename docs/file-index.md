# Индекс файлов lib/

> Автор: Цымбал Е. В. · апрель–июнь 2026  
> Сгенерировано скриптом `dart run tool/add_ecopulse_headers.dart`

Полный список исходников с кратким назначением каждого файла.

- `lib/app.dart` — *(шапка уже есть)*
- `lib/core/constants/api_config.dart` — *(шапка уже есть)*
- `lib/core/constants/api_keys_store.dart` — *(шапка уже есть)*
- `lib/core/constants/app_constants.dart` — *(шапка уже есть)*
- `lib/core/constants/bond_analytics_deep_link.dart` — *(шапка уже есть)*
- `lib/core/constants/market_catalog.dart` — *(шапка уже есть)*
- `lib/core/constants/moex_sectors.dart` — *(шапка уже есть)*
- `lib/core/content/courses/investor_basics/en_all.dart` — *(шапка уже есть)*
- `lib/core/content/courses/investor_basics/quizzes.dart` — *(шапка уже есть)*
- `lib/core/content/courses/investor_basics/ru_all.dart` — *(шапка уже есть)*
- `lib/core/content/courses/investor_basics/ru_part1.dart` — *(шапка уже есть)*
- `lib/core/content/courses/investor_basics/ru_part2.dart` — *(шапка уже есть)*
- `lib/core/content/courses/investor_basics/ru_part3.dart` — *(шапка уже есть)*
- `lib/core/content/courses/investor_basics/ru_part4.dart` — *(шапка уже есть)*
- `lib/core/content/courses/investor_basics/ru_part5.dart` — *(шапка уже есть)*
- `lib/core/content/course_chapter_map.dart` — *(шапка уже есть)*
- `lib/core/content/course_registry.dart` — *(шапка уже есть)*
- `lib/core/content/investor_basics_course.dart` — *(шапка уже есть)*
- `lib/core/errors/api_exception.dart` — *(шапка уже есть)*
- `lib/core/motion/app_motion.dart` — *(шапка уже есть)*
- `lib/core/services/assistant/assistant_context.dart` — *(шапка уже есть)*
- `lib/core/services/assistant/assistant_service.dart` — *(шапка уже есть)*
- `lib/core/services/assistant/faq_content.dart` — *(шапка уже есть)*
- `lib/core/services/assistant/gemini_client.dart` — *(шапка уже есть)*
- `lib/core/services/assistant/intent_router.dart` — *(шапка уже есть)*
- `lib/core/services/assistant/local_responder.dart` — *(шапка уже есть)*
- `lib/core/services/background_alert_service.dart` — *(шапка уже есть)*
- `lib/core/services/background_alert_worker.dart` — *(шапка уже есть)*
- `lib/core/services/backup_service.dart` — *(шапка уже есть)*
- `lib/core/services/biometric_auth_service.dart` — *(шапка уже есть)*
- `lib/core/services/bond_coupon_reminder_service.dart` — *(шапка уже есть)*
- `lib/core/services/error_reporting_service.dart` — *(шапка уже есть)*
- `lib/core/services/home_widget_service.dart` — *(шапка уже есть)*
- `lib/core/services/morning_digest_service.dart` — *(шапка уже есть)*
- `lib/core/services/notification_service.dart` — *(шапка уже есть)*
- `lib/core/theme/app_accent.dart` — *(шапка уже есть)*
- `lib/core/theme/app_backgrounds.dart` — *(шапка уже есть)*
- `lib/core/theme/app_palette.dart` — *(шапка уже есть)*
- `lib/core/theme/app_theme.dart` — *(шапка уже есть)*
- `lib/core/theme/app_tokens.dart` — *(шапка уже есть)*
- `lib/core/theme/app_typography.dart` — *(шапка уже есть)*
- `lib/core/theme/background_palette.dart` — *(шапка уже есть)*
- `lib/core/utils/async_refresh.dart` — *(шапка уже есть)*
- `lib/core/utils/bond_analytics.dart` — *(шапка уже есть)*
- `lib/core/utils/cache_status.dart` — *(шапка уже есть)*
- `lib/core/utils/chart_events.dart` — *(шапка уже есть)*
- `lib/core/utils/chart_normalize.dart` — *(шапка уже есть)*
- `lib/core/utils/chart_share.dart` — *(шапка уже есть)*
- `lib/core/utils/correlation_utils.dart` — *(шапка уже есть)*
- `lib/core/utils/digest_text.dart` — *(шапка уже есть)*
- `lib/core/utils/economic_radar.dart` — *(шапка уже есть)*
- `lib/core/utils/finance_math.dart` — *(шапка уже есть)*
- `lib/core/utils/formatters.dart` — *(шапка уже есть)*
- `lib/core/utils/inflation_chart_utils.dart` — *(шапка уже есть)*
- `lib/core/utils/inflation_math.dart` — *(шапка уже есть)*
- `lib/core/utils/market_list_utils.dart` — *(шапка уже есть)*
- `lib/core/utils/portfolio_backtest.dart` — *(шапка уже есть)*
- `lib/core/utils/portfolio_export.dart` — *(шапка уже есть)*
- `lib/core/utils/portfolio_math.dart` — *(шапка уже есть)*
- `lib/core/utils/rate_inflation_utils.dart` — *(шапка уже есть)*
- `lib/core/utils/scenario_math.dart` — *(шапка уже есть)*
- `lib/core/utils/sector_labels.dart` — *(шапка уже есть)*
- `lib/core/utils/timeline_builder.dart` — *(шапка уже есть)*
- `lib/core/utils/watchlist_report.dart` — *(шапка уже есть)*
- `lib/data/demo/demo_fixtures.dart` — *(шапка уже есть)*
- `lib/data/models/candle_point.dart` — *(шапка уже есть)*
- `lib/data/models/chart_period.dart` — *(шапка уже есть)*
- `lib/data/models/chat_message.dart` — *(шапка уже есть)*
- `lib/data/models/commodity_quote.dart` — *(шапка уже есть)*
- `lib/data/models/course.dart` — *(шапка уже есть)*
- `lib/data/models/course_quiz.dart` — *(шапка уже есть)*
- `lib/data/models/crypto_feed.dart` — *(шапка уже есть)*
- `lib/data/models/currency_rate.dart` — *(шапка уже есть)*
- `lib/data/models/inflation_point.dart` — *(шапка уже есть)*
- `lib/data/models/key_rate_point.dart` — *(шапка уже есть)*
- `lib/data/models/macro_event.dart` — *(шапка уже есть)*
- `lib/data/models/market_asset.dart` — *(шапка уже есть)*
- `lib/data/models/news_item.dart` — *(шапка уже есть)*
- `lib/data/models/portfolio_position.dart` — *(шапка уже есть)*
- `lib/data/models/price_alert.dart` — *(шапка уже есть)*
- `lib/data/models/price_point.dart` — *(шапка уже есть)*
- `lib/data/models/user_profile.dart` — *(шапка уже есть)*
- `lib/data/repositories/cbr_repository.dart` — *(шапка уже есть)*
- `lib/data/repositories/commodities_repository.dart` — *(шапка уже есть)*
- `lib/data/repositories/correlation_repository.dart` — *(шапка уже есть)*
- `lib/data/repositories/currency_repository.dart` — *(шапка уже есть)*
- `lib/data/repositories/inflation_repository.dart` — *(шапка уже есть)*
- `lib/data/repositories/market_repository.dart` — *(шапка уже есть)*
- `lib/data/repositories/news_repository.dart` — *(шапка уже есть)*
- `lib/data/services/api_client.dart` — *(шапка уже есть)*
- `lib/data/services/cache_service.dart` — *(шапка уже есть)*
- `lib/data/services/http_log_store.dart` — *(шапка уже есть)*
- `lib/data/services/retry_interceptor.dart` — *(шапка уже есть)*
- `lib/data/utils/moex_parser.dart` — *(шапка уже есть)*
- `lib/features/admin/admin_panel_screen.dart` — *(шапка уже есть)*
- `lib/features/alerts/price_alerts_screen.dart` — *(шапка уже есть)*
- `lib/features/analytics/compare_assets_screen.dart` — *(шапка уже есть)*
- `lib/features/analytics/correlation_screen.dart` — *(шапка уже есть)*
- `lib/features/asset_detail/asset_detail_screen.dart` — *(шапка уже есть)*
- `lib/features/assistant/assistant_fab.dart` — *(шапка уже есть)*
- `lib/features/assistant/assistant_sheet.dart` — *(шапка уже есть)*
- `lib/features/auth/pin_lock_screen.dart` — *(шапка уже есть)*
- `lib/features/auth/pin_setup_screen.dart` — *(шапка уже есть)*
- `lib/features/calculators/finance_calculators.dart` — *(шапка уже есть)*
- `lib/features/currency/currency_screen.dart` — *(шапка уже есть)*
- `lib/features/home/bond_home_card.dart` — *(шапка уже есть)*
- `lib/features/home/economic_brief.dart` — *(шапка уже есть)*
- `lib/features/home/economic_radar_card.dart` — *(шапка уже есть)*
- `lib/features/home/home_screen.dart` — *(шапка уже есть)*
- `lib/features/home/key_rate_detail_screen.dart` — *(шапка уже есть)*
- `lib/features/home/news_feed_card.dart` — *(шапка уже есть)*
- `lib/features/inflation/inflation_screen.dart` — *(шапка уже есть)*
- `lib/features/insights/macro_calendar_screen.dart` — *(шапка уже есть)*
- `lib/features/insights/timeline_screen.dart` — *(шапка уже есть)*
- `lib/features/learn/course_home_card.dart` — *(шапка уже есть)*
- `lib/features/learn/course_library_screen.dart` — *(шапка уже есть)*
- `lib/features/learn/course_quiz_screen.dart` — *(шапка уже есть)*
- `lib/features/learn/course_reader_screen.dart` — *(шапка уже есть)*
- `lib/features/learn/course_reader_shell.dart` — *(шапка уже есть)*
- `lib/features/markets/asset_note_field.dart` — *(шапка уже есть)*
- `lib/features/markets/asset_preview_sheet.dart` — *(шапка уже есть)*
- `lib/features/markets/bond_analytics_hero_title.dart` — *(шапка уже есть)*
- `lib/features/markets/bond_calendar_card.dart` — *(шапка уже есть)*
- `lib/features/markets/bond_calendar_screen.dart` — *(шапка уже есть)*
- `lib/features/markets/bond_calendar_sidebar.dart` — *(шапка уже есть)*
- `lib/features/markets/bond_event_list.dart` — *(шапка уже есть)*
- `lib/features/markets/bond_ladder_card.dart` — *(шапка уже есть)*
- `lib/features/markets/bond_ladder_screen.dart` — *(шапка уже есть)*
- `lib/features/markets/bond_yield_curve_table.dart` — *(шапка уже есть)*
- `lib/features/markets/markets_screen.dart` — *(шапка уже есть)*
- `lib/features/markets/market_asset_row.dart` — *(шапка уже есть)*
- `lib/features/markets/ofz_yield_curve_card.dart` — *(шапка уже есть)*
- `lib/features/markets/ofz_yield_curve_chart.dart` — *(шапка уже есть)*
- `lib/features/markets/ofz_yield_curve_screen.dart` — *(шапка уже есть)*
- `lib/features/markets/sector_heatmap.dart` — *(шапка уже есть)*
- `lib/features/onboarding/onboarding_screen.dart` — *(шапка уже есть)*
- `lib/features/portfolio/add_position_sheet.dart` — *(шапка уже есть)*
- `lib/features/portfolio/paper_portfolio_screen.dart` — *(шапка уже есть)*
- `lib/features/portfolio/portfolio_allocation_card.dart` — *(шапка уже есть)*
- `lib/features/portfolio/portfolio_allocation_mini.dart` — *(шапка уже есть)*
- `lib/features/portfolio/portfolio_allocation_pie.dart` — *(шапка уже есть)*
- `lib/features/portfolio/portfolio_home_card.dart` — *(шапка уже есть)*
- `lib/features/profile/profile_screen.dart` — *(шапка уже есть)*
- `lib/features/settings/home_layout_settings.dart` — *(шапка уже есть)*
- `lib/features/settings/profile_backup_widgets.dart` — *(шапка уже есть)*
- `lib/features/settings/settings_screen.dart` — *(шапка уже есть)*
- `lib/features/settings/widget_settings.dart` — *(шапка уже есть)*
- `lib/features/shared/app_actions.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/animated_value_text.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/app_background.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/app_card.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/app_hover.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/app_refresh_indicator.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/app_segmented_control.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/charts.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/empty_state.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/glass_card.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/last_updated_banner.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/loading_skeleton.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/metric_card.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/motion_widgets.dart` — *(шапка уже есть)*
- `lib/features/shared/widgets/section_header.dart` — *(шапка уже есть)*
- `lib/features/shell/app_gate.dart` — *(шапка уже есть)*
- `lib/features/shell/app_shell_shortcuts.dart` — *(шапка уже есть)*
- `lib/features/shell/main_shell.dart` — *(шапка уже есть)*
- `lib/main.dart` — *(шапка уже есть)*
- `lib/providers/accent_provider.dart` — *(шапка уже есть)*
- `lib/providers/admin_provider.dart` — *(шапка уже есть)*
- `lib/providers/alert_history_provider.dart` — *(шапка уже есть)*
- `lib/providers/api_keys_provider.dart` — *(шапка уже есть)*
- `lib/providers/app_providers.dart` — *(шапка уже есть)*
- `lib/providers/asset_notes_provider.dart` — *(шапка уже есть)*
- `lib/providers/assistant_provider.dart` — *(шапка уже есть)*
- `lib/providers/background_provider.dart` — *(шапка уже есть)*
- `lib/providers/base_currency_provider.dart` — *(шапка уже есть)*
- `lib/providers/commodities_provider.dart` — *(шапка уже есть)*
- `lib/providers/compact_home_provider.dart` — *(шапка уже есть)*
- `lib/providers/compare_assets_provider.dart` — *(шапка уже есть)*
- `lib/providers/correlation_provider.dart` — *(шапка уже есть)*
- `lib/providers/course_progress_provider.dart` — *(шапка уже есть)*
- `lib/providers/course_reader_prefs_provider.dart` — *(шапка уже есть)*
- `lib/providers/default_tab_provider.dart` — *(шапка уже есть)*
- `lib/providers/demo_mode_provider.dart` — *(шапка уже есть)*
- `lib/providers/feature_flags_provider.dart` — *(шапка уже есть)*
- `lib/providers/home_layout_provider.dart` — *(шапка уже есть)*
- `lib/providers/indices_provider.dart` — *(шапка уже есть)*
- `lib/providers/locale_provider.dart` — *(шапка уже есть)*
- `lib/providers/morning_digest_provider.dart` — *(шапка уже есть)*
- `lib/providers/news_provider.dart` — *(шапка уже есть)*
- `lib/providers/onboarding_provider.dart` — *(шапка уже есть)*
- `lib/providers/paper_portfolio_provider.dart` — *(шапка уже есть)*
- `lib/providers/price_alerts_provider.dart` — *(шапка уже есть)*
- `lib/providers/security_provider.dart` — *(шапка уже есть)*
- `lib/providers/stock_market_provider.dart` — *(шапка уже есть)*
- `lib/providers/theme_provider.dart` — *(шапка уже есть)*
- `lib/providers/user_profile_provider.dart` — *(шапка уже есть)*
- `lib/providers/watchlist_provider.dart` — *(шапка уже есть)*
- `lib/providers/widget_config_provider.dart` — *(шапка уже есть)*

