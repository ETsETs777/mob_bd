name: ecopulse_server
description: EcoPulse Home Server — LAN backend for profiles and chat.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^2.7.5
  bcrypt: ^1.1.3
  dart_jsonwebtoken: ^2.17.0
  uuid: ^4.5.1
  crypto: ^3.0.6
  path: ^1.9.1
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3
  postgres: ^3.5.12
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
