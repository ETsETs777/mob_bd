# EcoPulse Home Server

Dart package in `server/` — shelf + SQLite LAN backend.

```powershell
dart pub get
dart run bin/server.dart --host 0.0.0.0 --port 8081
```

See [../docs/home-server.md](../docs/home-server.md) for full setup.
